language: node_js
node_js:
  - "0.10"
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sudo sed -e'/^127\.0\.0\.1/a 127.0.0.1 localhost' /etc/hosts
before_script:
  - npm install -g bower
  - bower install
after_script:
  - npm run coveralls
