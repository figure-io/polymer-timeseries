/* global chai, describe, it */
'use strict';

// TESTS //

describe( 'polymer-chart-timeseries', function tests() {

	var expect = chai.expect,
		assert = chai.assert;

	it( 'should do something' );

	it( 'should do something else' );

});
